import { NextPage } from "next";
import { ReactElement } from "react";
import { UpdateCurrencyPage } from "../../_components/update-currency-page";

const Page: NextPage = (): ReactElement => {
  return <UpdateCurrencyPage />;
};

export default Page;
