import { NextPage } from "next";
import { ReactElement } from "react";
import { ItemsIn } from "./_component/items-in";

const Page: NextPage = (): ReactElement => {
  return <ItemsIn />;
};
export default Page;
